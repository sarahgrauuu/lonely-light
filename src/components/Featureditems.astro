---
---

<section>
  <hr />
  <div class="featuredmenu">
    <h2>Tilbud</h2>
    <h2 class="grey">Nyheder</h2>
    <h2 class="grey">Best Seller</h2>
  </div>
  <div class="grid">
    <slot />
  </div>
</section>

<style>
  hr {
    border-top: 1px solid;
    border-color: var(--sdGrey600);
  }

  section {
    margin-left: var(--sdSpacing20);
    margin-right: var(--sdSpacing20);
  }

  .featuredmenu {
    display: flex;
    flex-direction: row;
    justify-content: flex-start;
    gap: var(--sdHorizontalSpacingItemCardHorizontalSpacing);
  }

  h2 {
    font-family: var(--sdFontFamiliesOldvankau);
    font-size: var(--sdFontSize7);
  }

  .grey {
    color: var(--sdItemSpecialTextGrey);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(4,1fr);
    justify-items: center;
    gap: 25px;
  }
</style>
